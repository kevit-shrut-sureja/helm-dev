{
  "name": "helm-dev",
  "version": "0.4",
  "private": true,
  "description": "Local control panel for Nx monorepos: runs services, merges their logs, and resolves each line back to the source that printed it.",
  "type": "module",
  "main": "server.mjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "./helm-dev",
    "resume": "./helm-dev --resume",
    "reindex": "./helm-dev --reindex"
  },
  "dependencies": {},
  "devDependencies": {}
}

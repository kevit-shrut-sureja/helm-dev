# Built from the watched repo on first run; machine- and commit-specific.
.cache/

# Machine settings: repo paths, concurrency, buffer size.
settings.json

node_modules/

# Never intended here — helm-dev has no dependencies and reads no dotenv file,
# so nothing should ever write one. Ignored defensively in case something does.
.env
